- name: Deploy Kubernetes Master
  hosts: k8s-master
  roles:
    - rhbase
    - docker
    - kubernetes

- name: Deploy Kubernetes Nodes
  hosts: k8s-node
  vars:
    - kubernetes_role: "node"
  roles:
    - rhbase
    - docker
    - kubernetes    